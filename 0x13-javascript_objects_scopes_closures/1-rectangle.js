#!/usr/bin/node
class Rectangle {
  /**
   * Define an Rectangle with dimensions
   * @param {Number} w The value of the width
   * @param {Number} h The value of the height
   */
  constructor (w, h) {
    this.width = w;
    this.height = h;
  }
}

module.exports = Rectangle;
